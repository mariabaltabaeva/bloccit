<h1>New Sponsored Post</<h1>

  <div class="row">
    <div class="col-md-4">
      <p>Giidelines for sponsored posts</p>
      <ul>
        <li>Make sure it rhymes.</li>
        <li>Don't use the letter "A"</li>
        <li>The incessant use of hashtags will get you banned.</li>
      </ul>
    </div>

    <div class="col-md-8">
      <%= form_for [@topic, @sponsored_post] do |s| %>
      <div class="form-group">
        <%= s.label :title %>
        <%= s.text_field :title, class: 'form-control', placeholder: "Enter post title"%>
      </div>

      <div class="form-group">
        <%= s.label :body %>
        <%= s.text_area :body, class: 'form-control', placeholder: "Enter post body" %>
      </div>

      <div class="form-group">
        <%= s.submit "Save", class: 'btn btn-success' %>
      </div>
      <% end %>
    </div>
  </div>
