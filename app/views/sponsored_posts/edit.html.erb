<h1>Edit Sponsored Post</h1>

 <div class="row">
   <div class="col-md-4">
     <p>Guidelines for sponsored posts</p>
     <ul>
       <li>Make sure it rhymes.</li>
       <li>The incessant use of hashtags will get you banned.</li>
       <li>Make sure it has price.</li>
     </ul>
   </div>
   <div class="col-md-8">
     <%= form_for [@sponsored_post.topic, @sponsored_post] do |s| %>
       <div class="form-group">
         <%= s.label :title %>
         <%= s.text_field :title, class: 'form-control', placeholder: "Enter sponsored post title" %>
       </div>
       <div class="form-group">
         <%= s.label :body %>
         <%= s.text_area :body, rows: 8, class: 'form-control', placeholder: "Enter sponsored post body" %>
       </div>
       <div>
         <div class="form-group">
           <%= s.label :price %>
           <%= s.text_area :price, class: 'form-control', placeholder:"Enter price" %>
         </div>
       <div class="form-group">
         <%= s.submit "Save", class: 'btn btn-success' %>
       </div>
     <% end %>
   </div>
 </div>
